import { HashLoader } from "react-spinners";
import './loading.css'

export default function Loading(){
return (
    <div className="mr-loading-container">
        <HashLoader color="#FF8500" />
    </div>
)
}